# Environments

Deployable infrastructure lives here.

`aws/lab` is the current working environment. Azure environments will be added here later so AWS and Azure can be managed from the same repository without mixing their configuration.
