# Platform

This folder contains the pieces that support Terraform itself: remote-state bootstrap, workload identity and the HCP Terraform setup.

The goal is to keep platform plumbing separate from the infrastructure environments it manages.
