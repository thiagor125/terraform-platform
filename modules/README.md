# Modules

Reusable Terraform building blocks live here.

The current module is `aws/network`, which creates the networking used by the AWS lab. Keeping it separate from the environment makes it easier to reuse and evolve without duplicating infrastructure code.
