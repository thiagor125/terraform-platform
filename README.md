# Terraform Execution Platform

Central control plane for enterprise Terraform execution.

This repository owns reusable CI/CD workflows, cloud bootstrap, identity federation and platform standards. Engineers do not need local cloud credentials for official infrastructure changes; live repositories call these reusable workflows and authenticate through OIDC.
